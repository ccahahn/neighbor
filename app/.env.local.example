# Copy to .env.local and fill in a Google Maps JavaScript API key with
# Maps JavaScript API + Directions API enabled. Restrict the key to
# http://localhost:5173/* during development.
#
# Either variable name works (vite.config.js whitelists both prefixes):
GOOGLE_MAPS_API_KEY=
# VITE_GOOGLE_MAPS_API_KEY=
